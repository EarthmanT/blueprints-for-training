tosca_definitions_version: nativeedge_1_0
description: A simple terraform blueprint.

imports:
  - nativeedge/types/types.yaml
  - plugin:nativeedge-terraform-plugin

inputs:

  first_deployment:
    default: first

  expected_value:
    default: { get_capability: [{get_input: first_deployment}, "exposed_capability"] }

node_templates:

  terraform:
    type: nativeedge.nodes.terraform
    properties:
      resource_config:
        installation_source: https://releases.hashicorp.com/terraform/0.14.11/terraform_0.14.11_linux_amd64.zip

  cloud_resources_2:
    type: nativeedge.nodes.terraform.Module
    properties:
      resource_config:
        source:
          location: tf_module/terraform2.zip
        source_path: second
        variables:
          message: { get_input: ["expected_value", 0] }
    relationships:
      - target: terraform
        type: nativeedge.relationships.terraform.run_on_host

